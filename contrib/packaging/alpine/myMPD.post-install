#!/bin/sh

if [ -d /var/lib/mympd/ssl ]
then
  echo "Certificates already created"
else
  echo "Creating certificates"
  /usr/share/mympd/crcert.sh /var/lib/mympd/ssl
fi

echo "myMPD installed"
echo "Edit /etc/mympd.conf before starting myMPD"
